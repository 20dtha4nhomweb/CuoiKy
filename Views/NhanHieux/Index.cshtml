@model IEnumerable<CuoiKy.Models.NhanHieu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNhanHieu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNhanHieu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNH }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNH })
        </td>
    </tr>
}

</table>
